@model WebApp.Models.User

@{
    ViewBag.Title = "Login";
}

@*<h2>Login</h2>*@
<div class="container">
    <div class="row">
        <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
            <div class="card card-signin my-5">
                <div class="card-body">
                    @using (Html.BeginForm("Login", "Account"))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true)
                        <div class="container">
                            <fieldset>
                                <legend>Log In</legend>

                                <div class="editor-label">                                    
                                    @Html.LabelFor(model => model.EmailId, "User Name")
                                </div>
                                <div class="editor-field">
                                    @Html.TextBoxFor(model => model.EmailId, new { @class = "input-text", placeholder = "Username" })
                                    @Html.ValidationMessageFor(model => model.EmailId)
                                </div>

                                <div class="editor-label">
                                    @Html.LabelFor(model => model.Pwd, "Password")
                                </div>
                                <div class="editor-field">
                                    @Html.TextBoxFor(model => model.Pwd, new { @class = "input-text", placeholder = "Password" })
                                    @Html.ValidationMessageFor(model => model.Pwd)
                                </div>

                                <p>
                                    <input type="submit" value="Log In" class="btn" />
                                </p>
                            </fieldset>
                        </div>                        
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
